#include "main.h"
/**
 * _puts - takes in string
 * @str: string
 * Return: 0
 */
void rev_string(char *s)
{
	int i;

	int r;


	for (i = 0; s[i] != '\0'; i++)i

	
		for (r = i - 1; s[i] != '\0'; r--)
	
}
