#include "main.h"
/**
 * reverse_array - a function that reverse an array of integers
 * @a: first parameter
 * @n: second parameters
 * Return: always 0
 */
void reverse_array(int *a, int n)
{
	int i, b;

	for (i = 0; a[i] < n; i++)
	{
	}
	for (b = 0; a[b] < n; i--)
	{
		a[b] = n - 1;
	}
}
