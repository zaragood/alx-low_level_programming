#include "main.h"
#include <stdio.h>
/**
 * main - main entry
 * @argc: first parameter
 * @argv: second parameter
 *
 * Return: always 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
