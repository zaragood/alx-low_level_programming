#include "main.h"
#include <stdio.h>
/**
 * main - entry point
 * @argc: arguement count
 * @argv: argument command line
 *
 * Return: always 0
 */
int main(int argc, char __attribute__((unused)) *argv[])
{
	printf("%d\n", argc);
	return (0);
}
