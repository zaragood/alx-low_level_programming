#include "main.h"
/**
 * print_square - a function that prints a square
 * @size:is the size of the quare
 *
 * Return: always 0
 */
void print_square(int size)
{
	int i;
	int j;

	if (size <= 0)
	{
		_putchar('\n');
	}
}
