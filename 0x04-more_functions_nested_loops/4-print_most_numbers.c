#include "main.h"
/**
 * print_most_numbers - prints most numbers
 * void: returns nothing
 *
 * Return: always 0
 */

void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 0; i++)
	{
		_putchar((i) + '0');

		if (i != 2 && i != 4)
		{
			_putchar((i) + '0');
		}

	}
	 
}
