#include <stdio.h>
/**
 * main - Entry point
 * A program that uses a printf function to print text to console
 * Return: Always 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
